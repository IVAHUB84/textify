# Release Notes v1.11.1

- **Дата:** 2026-06-25
- **Тип релиза:** patch

## Кратко

Актуализированы пользовательские тексты под видео-канал (v1.11.0): `/start`, `/help` и
description/short профиля бота теперь упоминают распознавание речи из видео. Только копирайт —
поведение не меняется.

## Что нового

### Улучшения

- **`/start`** и **`/help`** дополнены пунктом про видео: «видео, кружочки и видео-файлы → распознаю
  речь из видео» (с уточнением, что это звуковая дорожка, а не текст в кадре).
- **Профиль бота** (`BOT_DESCRIPTION`, `BOT_SHORT_DESCRIPTION`) — «Голос, видео и фото → текст».
  Лимиты Telegram соблюдены (description 322/512, short 85/120).

### Исправления

- Нет (актуализация текстов).

## Изменения публичного контракта

- Только тексты `/start`, `/help` и метаданные профиля. Команды, поведение, лимиты, профиль поставки
  — без изменений. Новых env/зависимостей/миграций нет.

## Проверка

- `ruff check .` — без замечаний.
- `pytest` — 639 passed, 3 skipped.
- **Frontend / Playwright:** не применимо — Telegram-бот на Python.
